Read `AGENTS.md` and all files referenced by it. Then read `docs/plans/BOOTSTRAP_PLAN.md`.

Implement Milestone 0 and Milestone 1 only:

1. Create an Android Studio project using Kotlin, Jetpack Compose, Gradle, CMake, and a C++20 NDK library.
2. Add a host-buildable C++ DSP library so core algorithms can be tested without Android.
3. Implement core data types, WAV fixtures, STFT/iSTFT, deterministic tests, benchmarks, and result export.
4. Do not implement live anti-noise output, Bluetooth support, or microphone monitoring yet.
5. Add scripts or Gradle tasks for build, test, lint, and host benchmarks.
6. Update `docs/plans/BOOTSTRAP_PLAN.md` continuously with progress, decisions, evidence, and remaining work.
7. Stop after Milestone 1 acceptance criteria pass. Report exact commands run and test results.
